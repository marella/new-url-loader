This example uses `@svgr/webpack` with `new-url-loader` instead of `url-loader`. See [`webpack.config.js`](webpack.config.js) and [`src/index.js`](src/index.js).

**Install**

```sh
npm install
```

**Build**

```sh
npm run build
```

**Serve**

```sh
npm run serve
```
